import 'package:flutter/material.dart';
import 'package:smartsoil/Feature/detact/presentation/widgets/detact_view_body.dart';

class DetactView extends StatelessWidget {
  const DetactView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: DetactViewBody(),
    );
  }
}
